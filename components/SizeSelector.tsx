 "use client"; export default function SizeSelector({sizes,value,onChange}:{sizes:{label:string;price:number}[];value:string;onChange:(v:string)=>void}){return <div className="flex flex-wrap gap-2">{sizes.map(s=><button key={s.label} onClick={()=>onChange(s.label)} className={`rounded-full border px-4 py-2 text-sm ${value===s.label?"border-tawar-gold bg-tawar-gold text-white":"border-black/10"}`}>{s.label}</button>)}</div>}