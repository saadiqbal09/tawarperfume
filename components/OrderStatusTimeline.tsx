const steps=[["pending_payment","Placed"],["payment_confirmed","Payment Confirmed"],["packing","Packing"],["dispatched","Dispatched"],["delivered","Delivered"]]; export default function OrderStatusTimeline({status}:{status:string}){const current=Math.max(0,steps.findIndex(s=>s[0]===status));return <ol className="space-y-4">{steps.map(([key,label],i)=><li key={key} className="flex items-center gap-4"><span className={`flex h-9 w-9 items-center justify-center rounded-full text-sm ${i<=current?"bg-tawar-gold text-white":"bg-tawar-cream text-black/35"}`}>{i+1}</span><div><p className={i<=current?"font-medium":"text-black/35"}>{label}</p>{i===current&&<p className="text-xs text-black/50">Current status</p>}</div></li>)}</ol>}