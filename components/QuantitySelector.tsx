 "use client"; export default function QuantitySelector({value,onChange}:{value:number;onChange:(n:number)=>void}){return <div className="inline-flex items-center rounded-full border border-black/10"><button className="px-4 py-2" onClick={()=>onChange(Math.max(1,value-1))}>−</button><span className="min-w-8 text-center">{value}</span><button className="px-4 py-2" onClick={()=>onChange(value+1)}>+</button></div>}