import type {Product} from "@/lib/types"; export default function FragranceNotes({product}:{product:Product}){return <div className="grid gap-4 md:grid-cols-3">{[["Top Notes",product.top_notes],["Heart Notes",product.heart_notes],["Base Notes",product.base_notes]].map(([label,notes])=><div key={String(label)} className="rounded-2xl bg-tawar-cream p-5"><h3 className="text-xs uppercase tracking-luxury text-tawar-gold">{label}</h3><p className="mt-3 font-display text-lg">{(notes as string[]).join(" · ")}</p></div>)}</div>}