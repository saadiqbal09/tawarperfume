 "use client"; import {useState} from "react"; import type {Order} from "@/lib/types"; import {formatINR} from "@/lib/utils"; export default function AdminOrderRow({order,onApproved}:{order:Order;onApproved:()=>void}){const[l,setL]=useState(false);async function approve(){setL(true);await fetch("/api/approve",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({orderId:order.order_id})});setL(false);onApproved()}return <tr className="border-b border-black/5"><td className="p-3 font-medium">{order.order_id}</td><td className="p-3">{order.customer_name}</td><td className="p-3">{order.customer_phone}</td><td className="p-3">{formatINR(order.total)}</td><td className="p-3">{order.status}</td><td className="p-3">{order.status==="pending_payment"&&<button disabled={l} onClick={approve} className="rounded-full bg-tawar-gold px-3 py-2 text-xs text-white">{l?"…":"Approve"}</button>}</td></tr>}