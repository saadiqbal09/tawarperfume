 "use client"; import {useEffect,useState} from "react"; import Link from "next/link"; const slides=[["New Arrivals","A signature scent for every journey.","/products"],["Master Combo","The complete TAWAR cabin ritual.","/gifts-combos"],["Best Sellers","Refined fragrances. Effortless presence.","/products"]]; export default function HeroSlider(){const[i,setI]=useState(0);useEffect(()=>{const t=setInterval(()=>setI(x=>(x+1)%slides.length),5000);return()=>clearInterval(t)},[]);return <section className="relative overflow-hidden rounded-[2rem] bg-tawar-black text-white"><div className="min-h-[430px] px-7 py-20 md:px-16 md:py-28"><p className="text-xs uppercase tracking-[.3em] text-tawar-gold">TAWAR Fragrance House</p><h1 className="mt-5 max-w-3xl font-display text-5xl leading-tight md:text-7xl">{slides[i][0]}</h1><p className="mt-5 max-w-xl text-lg text-white/65">{slides[i][1]}</p><Link href={slides[i][2]} className="mt-9 inline-flex rounded-full bg-tawar-gold px-7 py-3 text-sm uppercase tracking-wider">Explore</Link></div><div className="absolute bottom-7 right-7 flex gap-2">{slides.map((_,n)=><button aria-label={`Slide ${n+1}`} key={n} onClick={()=>setI(n)} className={`h-2 w-8 rounded-full ${n===i?"bg-tawar-gold":"bg-white/25"}`}/>)}</div></section>}