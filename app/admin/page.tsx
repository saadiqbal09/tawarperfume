 "use client"; import {useState} from "react"; import {useRouter} from "next/navigation"; export default function Admin(){const[p,setP]=useState("");const[e,setE]=useState("");const r=useRouter();async function submit(ev:React.FormEvent){ev.preventDefault();const x=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password:p})});if(x.ok)r.push("/admin/dashboard");else setE("Invalid password");}return <section className="mx-auto max-w-md"><h1 className="font-display text-5xl">Admin</h1><form onSubmit={submit} className="mt-8 rounded-2xl bg-white p-6 shadow-tawar"><input type="password" required value={p} onChange={e=>setP(e.target.value)} placeholder="Admin password" className="w-full rounded-xl border border-black/10 px-4 py-3"/><button className="mt-3 w-full rounded-xl bg-tawar-black py-3 text-white">Enter dashboard</button>{e&&<p className="mt-3 text-sm text-red-600">{e}</p>}</form></section>}