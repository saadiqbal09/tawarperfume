import {redirect} from "next/navigation"; import {cookies} from "next/headers"; import {createClient} from "@/lib/supabase/server"; import AdminOrderRow from "@/components/AdminOrderRow"; export default async function Dashboard(){const c=await cookies();if(c.get("tawar_admin")?.value!=="1")redirect("/admin");const s=await createClient();const orders=s?(await s.from("orders").select("*").order("created_at",{ascending:false})).data??[]:[];return <section><div className="flex items-end justify-between"><div><p className="text-xs uppercase tracking-luxury text-tawar-gold">Operations</p><h1 className="mt-2 font-display text-5xl">Orders</h1></div></div><div className="mt-8 overflow-x-auto rounded-2xl bg-white shadow-tawar"><table className="w-full text-left text-sm"><thead><tr className="border-b border-black/10"><th className="p-3">Order</th><th className="p-3">Customer</th><th className="p-3">Phone</th><th className="p-3">Total</th><th className="p-3">Status</th><th className="p-3">Action</th></tr></thead><tbody>{orders.map(o=><AdminOrderRow key={o.id} order={o} onApproved={()=>{}}/>)}</tbody></table>{!orders.length&&<p className="p-8 text-center text-black/45">No Supabase orders yet. Demo mode is active when Supabase is not configured.</p>}</div></section>}