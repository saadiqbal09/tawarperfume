import {getProducts} from "@/lib/data"; import ProductGrid from "@/components/ProductGrid"; export default async function Gifts(){const products=(await getProducts()).filter(p=>p.sku.includes("CMB"));return <section><p className="text-xs uppercase tracking-luxury text-tawar-gold">Thoughtful gifting</p><h1 className="mt-2 font-display text-5xl">Gifts & Combos</h1><div className="my-10 rounded-[2rem] bg-tawar-black p-10 text-white"><p className="text-xs uppercase tracking-luxury text-tawar-gold">Signature set</p><h2 className="mt-3 font-display text-5xl">Master Combo</h2><p className="mt-4 max-w-xl text-white/60">A complete fragrance ritual for the driver who appreciates the details.</p></div><ProductGrid products={products}/></section>}