import {NextResponse} from "next/server"; import {getOrder} from "@/lib/data"; export async function GET(req:Request,{params}:{params:Promise<{orderId:string}>}){const {orderId}=await params;const phone=new URL(req.url).searchParams.get("phone")??undefined;const o=await getOrder(orderId==="lookup"?"":orderId,phone);if(!o)return NextResponse.json({error:"Order not found"},{status:404});return NextResponse.json(o);}