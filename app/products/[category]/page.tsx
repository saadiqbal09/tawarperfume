import {getProducts} from "@/lib/data"; import ProductGrid from "@/components/ProductGrid"; export default async function Category({params}:{params:Promise<{category:string}>}){const {category}=await params;const products=await getProducts(category);return <section><p className="text-xs uppercase tracking-luxury text-tawar-gold">Collection</p><h1 className="mt-2 font-display text-5xl capitalize">{category.replace("-"," ")}</h1><div className="mt-8"><ProductGrid products={products}/></div></section>}